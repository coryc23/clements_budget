// =============================================================
// Supabase connection config
// =============================================================
// Fill in the two values below with the credentials from your
// Supabase dashboard. Both are SAFE to commit to git — the
// publishable key respects Row Level Security.
//
// Where to find them:
//   1. Supabase dashboard → click "Connect" button at the top
//   2. Or: Project Settings → API Keys (use "Publishable" — NOT
//      "Secret"; the secret key bypasses RLS and must never go
//      in frontend code)
// =============================================================

window.SUPABASE_CONFIG = {
  url:             'PASTE_YOUR_PROJECT_URL_HERE',
  publishableKey:  'PASTE_YOUR_PUBLISHABLE_KEY_HERE'
};
